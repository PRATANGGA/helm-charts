apiVersion: v1
kind: Service
metadata:
  name: {{ include "infra-doks.fullname" . }}
  labels:
    {{- include "infra-doks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infra-doks.selectorLabels" . | nindent 4 }}
